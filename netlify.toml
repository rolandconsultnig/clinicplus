[build]
  publish = "static"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  # VITE_API_URL will be set in Netlify dashboard environment variables

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
